#[allow(dead_code)]
pub enum Flight{
   Economy,
   Business,
   First,
}

#[allow(dead_code)]
pub enum Color {
    Red,
    Green,
    Blue
}

#[allow(dead_code)]
pub enum HouseLocation {
    Number(i32),
    Name(String),
    Unknown
} 
